# Alembic config for the Cards API schema. The database URL is never stored
# here — it comes from CARDS_MIGRATIONS_DATABASE_URL (deploy) or is injected
# programmatically (tests). Migrations run as the schema owner, NOT as the
# cards_app runtime role (see docs/adr/ADR-2026-07-16-cards-api-postgres-rls.md).
[alembic]
script_location = migrations
sqlalchemy.url =
