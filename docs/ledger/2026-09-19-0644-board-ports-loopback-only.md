# 2026-09-19 06:44 CDT - Board ports loopback only (backend unpublished, frontend 127.0.0.1)
- **Who:** session Seven Of Nine (Chartroom feature manager), at the request of dev-44 via the Orchestrator.
- **Change:** in `docker-compose.gantry.yml` the backend no longer publishes `4070:4070` (now `expose: 4070` only), and the frontend publishes `127.0.0.1:8110:80` instead of `8110:80`. `docs/board/GANTRY_DEPLOY.md` is updated to match (expected render diffs, verify override on loopback) and its `git archive` example uses `-c core.autocrlf=false`.
- **Why:** Docker Desktop's inbound firewall allow rule (`com.docker.backend.exe`, any port, any remote address) exposes every `0.0.0.0` publish to the LAN and tailnet, so the board backend and frontend were reachable directly, skipping demo-proxy and Cloudflare. Nothing on the host uses 4070: the frontend nginx reaches the backend as `http://backend:4070` on the compose network. demo-proxy reaches 8110 through `host.docker.internal`, which cloudflare-config #18 verified still works with a loopback bind. Rejected alternative: keep 4070 on loopback; there is no consumer, so publishing it only adds surface.
- **State after:** `docker compose -p gantry-render-check -f docker-compose.gantry.yml config` renders no backend host port and `host_ip: 127.0.0.1` on 8110. Live Gantry is unchanged until #68 is deployed (Drew-gated); the live 8d43b7a containers still publish on 0.0.0.0 until then.
- **Refs:** PR #68; cloudflare-config #18; dev-44's comment on #68.
